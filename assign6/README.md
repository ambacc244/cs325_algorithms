Following commend lines allow to run bin.cpp file.
Assume, there is bin.txt file in the same directory.
txt file name should be bin.txt.

Command lines to run bin.cpp
	g++ -o run bin.cpp
	./run

Then, it will return bin.out file
